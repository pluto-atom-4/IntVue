<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="IntVue.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://microsoft.com"
    xmlns:local="using:IntVue"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    x:DataType="local:ViewModels.InterviewViewModel"> <!-- 👈 1. Added explicit compilation data type -->

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Preview host -->
        <controls:MediaPlayerElement x:Name="PreviewControl" AreTransportControlsEnabled="False" Grid.Row="0" />

        <!-- Controls -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Center" Margin="10">
            <Button x:Name="BtnPreview" Content="Start Preview" Click="BtnPreview_Click" AutomationProperties.Name="PreviewButton" />
            <Button x:Name="BtnRecord" Content="Start Recording" Click="BtnRecord_Click" AutomationProperties.Name="RecordButton" Margin="10,0,0,0" />
        </StackPanel>

        <!-- Consent / status -->
        <TextBlock x:Name="TxtConsent" Text="By enabling camera/microphone you consent to local recording." Grid.Row="2" TextWrapping="Wrap" Margin="10" />
    </Grid>
</Page>
